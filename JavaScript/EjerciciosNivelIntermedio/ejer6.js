//Declarar un array con 3 nombres.
const arr = ["Juan", "Pedro", "Pepe"];

//Usar bucle for e imprimir los nombres por consola

for (let i = 0; i < 3; i++) {
  console.log("Nombre " + (i + 1) + ": " + arr[i]);
}
